# Third-party notices

Agentic Context Service is Apache-2.0 licensed. Runtime and development dependencies retain their
respective licenses. The repository does not redistribute model weights. The optional
`all-MiniLM-L6-v2` demo adapter downloads model artifacts under the model publisher's terms when a
user explicitly runs it.

Generate and inspect the CI SBOM and dependency audit before each release. Image and package
versions are pinned by the lockfile and Compose file; this notice is informational and does not
replace the licenses shipped by dependencies.
